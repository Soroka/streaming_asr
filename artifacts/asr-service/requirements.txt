# ── T-one ASR Service — CPU requirements ─────────────────────────────────────
#
# NOTE: the tone package itself is NOT on PyPI.
#       In Docker it is installed from the cloned GitHub repo in the builder
#       stage (see Dockerfile).  In the dev workflow, start.sh installs it
#       from GitHub on first run.
#
# numpy must stay <2 — binary compatibility with torch 2.2.2
numpy==1.26.4

# ── PyTorch (CPU-only build) ──────────────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchaudio==2.2.2+cpu

# ── HuggingFace transformers ──────────────────────────────────────────────────
# Must stay <5.0 — transformers 5.x requires PyTorch >= 2.4
transformers>=4.30.0,<5.0.0

# ── tone package dependencies ─────────────────────────────────────────────────
# onnxruntime: imported at module level by tone/onnx_wrapper.py even when not
#              used for inference (our service uses the PyTorch backend).
onnxruntime==1.26.0

# pyctcdecode + kenlm: CTC beam-search decoder used by BeamSearchCTCDecoder.
# kenlm requires cmake + g++ at build time (provided in the Docker builder stage
# and in the dev environment via system packages).
pyctcdecode>=0.2.0,<1.0.0
kenlm>=0.2.0,<1.0.0

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.136.1
uvicorn[standard]==0.47.0
starlette==1.0.1
httpx==0.28.1
anyio==4.13.0
python-multipart==0.0.20

# ── Model download ────────────────────────────────────────────────────────────
huggingface_hub==1.16.1

# ── Audio I/O & resampling ────────────────────────────────────────────────────
librosa==0.11.0
soundfile==0.13.1
scipy==1.17.1

# ── Preprocessing ─────────────────────────────────────────────────────────────
webrtcvad==2.0.10
noisereduce==3.0.3

# ── Test client ───────────────────────────────────────────────────────────────
websockets==16.0

# ── Benchmark script (benchmark.py) ──────────────────────────────────────────
datasets==4.8.5
tqdm==4.67.3
